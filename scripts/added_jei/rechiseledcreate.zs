import crafttweaker.api.text.Component;
import mods.jeitweaker.Jei;

Jei.addIngredientInformation(
  <item:rechiseledcreate:mechanical_chisel>, 
  Component.literal(
    "Uses rotational power to automate Chisel recipes. It can be set with a filter item to output."
  )
);
